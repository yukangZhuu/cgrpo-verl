**1. AdaBack (arXiv:2506.18110)**
- **出发点（痛点与背景）：** 在长序列生成的推理任务中，标准的强化学习（RL）面临奖励稀疏和输出空间呈指数级爆炸的问题。如果推理链过长，RL往往只能强化模型已有的行为，难以探索并发现初始概率极低的新解法；而监督微调（SFT）又需要大量昂贵的专家演示数据，难以规模化。
- **方法：** 提出了一种自适应回溯（Adaptive Backtracking, 简称AdaBack）的“样本级”课程学习算法。该算法在训练时不要求模型从头生成完整解答，而是提供目标输出的“部分前缀”。根据模型过去的奖励信号，动态调整每个样本的前缀暴露长度，让模型通过条件化正确的中间步骤，逐步学会完成整个推理链。
- **结论：** AdaBack 能够可靠地解决传统RL无法处理的长序列推理难题。它显著提高了 pass@k 指标，表明模型真正学会了发现新解法，并且在符号格式偏移和更长视距的推理任务中展现出强大的泛化能力。
- **贡献：** 开辟了介于SFT和RL之间的“部分监督”新范式，证明了样本级课程学习能让模型掌握全新的推理能力，为解决长序列依赖问题提供了有效途径。

**2. LILO (NeurIPS 2025, arXiv:2502.12272)**
- **出发点（痛点与背景）：** RL常用于大模型推理能力的后训练，但现有RL方法在遇到“太难”（模型总是失败，无奖励信号）或“太简单”（模型总是成功，无学习价值）的样本时效率极低。人类为了构建难度适宜的数据集往往需要耗费大量精力。
- **方法：** 提出了 LILO（Learnability Improves LLMs Optimally）算法。该方法通过算法自动识别能提供最高效训练的样本，即优先训练那些“成功率方差最大”（模型时而成功、时而失败）的样本，这种动态的中等难度指标被称为“可学习性”（Learnability）。
- **结论：** 实验表明，优先训练具有高“可学习性”的问题能够持续提升模型的最终测试准确率。在达到同等性能的情况下，最多可减少 3 倍的训练步数（即大幅缩短训练时间）。
- **贡献：** 从理论上证明了 LILO 能够最大化模型的预期改进。提供了一种可扩展的数据筛选指标，使LLM智能体能够自主扩展人类知识边界，摆脱了对人工标定数据难度的依赖。

**3. Hard Examples (arXiv:2508.14094)**
- **出发点（痛点与背景）：** 为大语言模型微调收集高质量训练样本成本高昂，实际的标注预算往往严重限制了数据量。在使用 GRPO 等强化学习算法进行后训练时，样本的“难度”如何影响训练效果尚不明确。
- **方法：** 研究了在严格的标注预算下，样本难度（简单、中等、困难、随机）对 GRPO 训练效果的影响。作者对比了使用不同难度策略筛选数据后，模型在多项推理任务上的表现。
- **结论：** 仅在“最难的10%样本”（即基础模型最常失败的样本）上进行训练，能带来高达 47% 的性能飞跃；而简单样本仅带来 3-15% 的微小提升。这是因为 GRPO 需要结果的方差来产生学习信号，困难样本能在训练中保持成功与失败交替的混合状态，从而最大化学习价值。
- **贡献：** 为预算受限条件下的数据收集提供了明确的指导方针：应优先收集并标注基础模型“最不擅长”的困难样本，因为它们是驱动 GRPO 微调效果的核心动力。

**4. Goldilocks RL (arXiv:2602.14868)**
- **出发点（痛点与背景）：** 大模型在海量语料上训练时，为了评估样本难度而反复遍历数据会严重浪费宝贵的GPU算力。经典的课程学习通常根据静态的复杂度对数据排序，但这并不符合特定模型在训练过程中能力的动态变化，容易陷入奖励稀疏的困境。
- **方法：** 提出了一种教师驱动的数据采样策略——Goldilocks（金发姑娘原则）。在学生模型使用 GRPO 训练的同时，一个并行的教师模型会持续评估学生当前的能力，并为学生挑选“难度适中”（既不太简单也不太难）的问题。教师模型会根据学生动态的表现不断自我调整筛选策略。
- **结论：** 在相同的计算算力预算下，采用 Goldilocks 数据采样策略显著提升了标准 GRPO 训练模型的推理性能，有效避免了算力在无效（太难或太简单）样本上的浪费。
- **贡献：** 提出了一种新颖的动态“教师-学生”架构，用于在强化学习中进行实时难度估计和数据选择。它摒弃了静态的数据集排序，优化了计算效率，成功缓解了大规模RL场景下的数据低效问题。

**5. R3 (arXiv:2402.05808)**
- **出发点（痛点与背景）：** 将RL应用于复杂推理的核心挑战在于奖励信号。结果监督（只看最终答案）提供的奖励过于稀疏，无法指出错误发生的具体步骤；而过程监督（按步骤给奖励）虽然能提供逐步信号，却需要极其昂贵且耗时的人工过程标注。
- **方法：** 提出了 R3（Learning Reasoning through Reverse Curriculum RL，基于逆向课程强化学习的推理学习）。该方法仅使用结果监督信号，它从正确的演示数据出发，将模型推理的“起始状态”从演示的末尾逐渐“倒推”向开头进行训练。
- **结论：** 这种逆向滑动策略让模型在各个阶段的探索都变得更容易，建立了一个逐步递进的课程。它使得即使只有结果监督，模型也能获得类似步骤级别的信号并精确纠错。在多项推理任务上，该方法平均超越基线 RL 方法 4.1 分。
- **贡献：** 巧妙地仅用低成本的“结果监督”就达到了昂贵“过程监督”的效果。为大模型提供了一种高效的逆向课程学习策略，在大幅节省人工标注成本的前提下，显著增强了模型的分布推理与纠错能力。